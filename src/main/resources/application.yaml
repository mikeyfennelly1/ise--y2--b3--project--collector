spring:
  application:
    name: b3proj
  nats:
    port: ${NATS_PORT:4222}
    topic-name: desktop-sysinfo
    android-topic-name: android-device-metrics
    host: ${NATS_HOST:localhost}

logging:
  level:
    root: ${COLLECTOR_LOG_LEVEL:DEBUG}
server:
  port: ${COLLECTOR_LISTEN_PORT:8081}
collector:
  id: ${COLLECTOR_ID:dev}
